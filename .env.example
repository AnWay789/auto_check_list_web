# Django settings
DEBUG=True
SECRET_KEY=django-insecure-g)obvw42&a!(e6nt57u8ek162b@rw_n7li!h&c_p$38jj6=9-#
ALLOWED_HOSTS=localhost,127.0.0.1,web
DJANGO_URL=web:8000
# Внешний URL для генерации ссылок, доступных из браузера (для fake_url)
DJANGO_EXTERNAL_URL=localhost:8000

# Суперпользователь Django (создаётся при первом старте контейнера web, если заданы USERNAME и PASSWORD)
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=your-secure-password

# Database (для Docker с PostgreSQL)
POSTGRES_DB=auto_check_list
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
DATABASE_URL=postgresql://postgres:postgres@db:5432/auto_check_list

# Celery
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# Lighthouse → ELK (опционально): если заданы, результаты Lighthouse отправляются в ELK
# ELK_URL=https://10.222.0.3:9200/runner-vm-bots-logs/_doc/
# ELK_USER=fluent_bit_system
# ELK_PASSWORD=your-elk-password
# ELK_VERIFY_SSL=false

# Telegram Bot
# Для бота, запущенного на хосте (не в Docker), используйте host.docker.internal:8001
# Для бота в Docker (сервис `telegram_bot` из docker-compose) используйте имя сервиса: telegram_bot:8001
TELEGRAM_URL=telegram_bot:8001

# Настройки телеграм-бота (используются сервисом telegram_bot)
# TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID обязательны для запуска бота
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-telegram-chat-id
